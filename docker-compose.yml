services:
  mongodb:
    image: mongo:7
    container_name: memorynest-mongo
    ports:
      - '27017:27017'
    volumes:
      - memorynest_mongo_data:/data/db
    restart: unless-stopped

volumes:
  memorynest_mongo_data:
